paho-mqtt==1.6.1
pyyaml==6.0.1

# Protocol server libraries (install as needed)
pymodbus==3.11.1           # Modbus TCP server
opcua==0.98.13            # OPC UA server

# Optional protocol libraries for future expansion
# snap7==1.3              # Siemens S7 server (requires snap7 C library)
# cpppo==4.0.7            # EtherNet/IP server

