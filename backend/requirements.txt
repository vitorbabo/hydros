# 3.13 rewrote ModbusServerContext/ModbusDeviceContext into deprecated SimData/SimDevice
# shims that no longer support live mutation via subscript/setValues() (see
# protocols/modbus_handler.py server mode), so stay below that until it's migrated.
pymodbus>=3.11.1,<3.13
pyyaml>=6.0
paho-mqtt>=2.1.0
jsonschema
opcua
python-snap7>=1.3
influxdb-client>=1.44.0
python-dotenv>=1.0.0
aiohttp>=3.10.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
pytest-aiohttp>=1.0.5